<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="view_stock_configuration" model="ir.ui.view">
        <field name="name">Stock settings: quantity available to promise</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="stock.res_config_settings_view_form" />
        <field name="arch" type="xml">
            <xpath expr="//block[@id='schedule_info']" position="after">
                <block title="Stock available to promise" name="available_info">
                    <setting
                        id="setting_stock_available_mrp_based_on"
                        help="Choose the product field that will be used to compute the production potential. The components of each BoM product will be queried for this field to determine how many items can be manufactured. If empty, Quantity On Hand is used. Note that only the quantity fields have meaning for computing stock."
                    >
                        <field name="stock_available_mrp_based_on" />
                    </setting>
                </block>
            </xpath>
        </field>
    </record>
</odoo>
